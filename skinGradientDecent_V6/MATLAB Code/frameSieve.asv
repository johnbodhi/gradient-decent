function [ Y ] = frameSieve( X )

    global RA Nr Mr

    A = 2 * size(RA,3); M = Nr*Mr;

    Y = X;

    for i = 1:1:A-1
        
        Y = cat( 1, Y, X );
    end

    uu = 1;
    for i = 1:M:size(Y,1)

        Y(,4) = uu; 
        
        uu = uu + 1;
    end
end